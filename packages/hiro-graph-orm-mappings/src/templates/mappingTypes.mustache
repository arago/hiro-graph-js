import { GraphVertex, IDefinition } from "@hiro-graph/orm";

export interface I{{name}} extends IDefinition {
{{#toTypes}}required{{/toTypes}}
{{#toTypes}}optional{{/toTypes}}
{{#toTypes}}relations{{/toTypes}}
}

declare type Relations = keyof I{{name}}["relations"];
declare type Props = {{{props}}};

export class {{name}}Vertex extends GraphVertex<Relations, Props> {

}

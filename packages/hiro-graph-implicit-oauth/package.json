{
  "name": "hiro-graph-implicit-oauth",
  "version": "0.2.6",
  "description": "Customisable Implicit Oauth",
  "main": "./lib/index.js",
  "scripts": {
    "build": "rm -rf lib/* && BABEL_ENV=production babel src --out-dir lib --source-maps",
    "prepublish": "npm run build",
    "test": "echo no tests"
  },
  "author": "Brendan Moore <bmoore@arago.co>",
  "license": "MIT",
  "dependencies": {
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-jest": "^20.0.3",
    "babel-preset-hiro": "^0.2.6",
    "jest": "^20.0.4"
  },
  "babel": {
    "presets": [
      [
        "hiro",
        {
          "library": true
        }
      ]
    ]
  }
}
